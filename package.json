{
  "name": "gulp-imprint",
  "version": "0.0.0",
  "description": "Set of configurable gulp tasks to release and publish nodejs apps and libraries",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "zzz"
  },
  "author": "Aleksandr 'Aux' Ivanov <aux@hexmode.org> (http://hexmode.org/)",
  "license": "BSD-2-Clause",
  "dependencies": {
    "any-shell-escape": "^0.1.1",
    "del": "^3.0.0",
    "gulp": "^3.9.1",
    "gulp-bump": "^3.1.0",
    "run-sequence": "^2.2.1"
  }
}
